# Copy this file to .env.development or .env.production and fill in real values.
# Neither .env.development nor .env.production should be committed to git.

# ── Server ───────────────────────────────────────────────────
PORT=3001
JWT_SECRET=

# ── Database (MySQL) ─────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chickenbroadv
DB_USER=root
DB_PASSWORD=
DB_POOL_MAX=10
DB_POOL_MIN=2
